#include<stdio.h>
#include<conio.h>

main()
{       /* sum of first and last digit of a number */

	int firstdigit,lastdigit,n,sum;
	clrscr();

		printf("Enter any Number :");
		scanf("%d",&n);

		lastdigit=n%10;

		while(n>=9)
		{
			n=n/10;
		}
		firstdigit=n;
		sum=firstdigit+lastdigit;
		printf("Sum of First Digit & Last Digit id : %d",firstdigit+lastdigit);
	getch();

}