#include<stdio.h>
#include<conio.h>

main()
{
	char c;
	clrscr();

	printf("Enter any Character: ");
	scanf("%c",&c);

	if((c >= 'a'  && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		printf("%c is an Alphabet.....\n",c);
	}
	else if(c >= '0' && c <= '9')
	{
		printf("%c is a Digit.....\n",c);
	}
	else
	{
		printf("%c is a Special Character.....\n",c);
	}


	getch();
}